using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TYPE_FURNITURE
{
    DUNGEON_HALL,
    FARM,
    BED,
    TENT,
    CRYSTAL_BALL,
    PICKAXE,
    ANVIL,
    TRAINING_CENTER,
    TELEPORT
}

public enum EFFECT_FURNITURE
{
    POPULATION,
    FOOD,
    SOLDIER,
    MANA,
    DIGGING,
    MAKE_EQUIPMENT,
    GET_JOB
}

public class Furniture : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
